import React from 'react';
import {useState, useEffect} from 'react'
function Discover({bookData}) {
    const [selectedBooks, setSelectedBooks] = useState([])
    return (
        <div>
            
        </div>
    );
}

export default Discover;